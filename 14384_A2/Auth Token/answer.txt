By anlayzing code u see that a random number is generated and compared with the input number. Now, this random number is always same( due to use of srand()). By guesssing this number right file_size_utility function gets called. Here, is the tricky part . we have to redefine the definition of du now. Update it in $PATH environment variable. Change the permission of all the files in the path provided.The script for du is provided. which executes cat flag.txt.
