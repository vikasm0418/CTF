Here, the flag.txt file contained the morse code. Converting this into code into string outputs some decimal values.Analyzing which reveals that 628 is repeated after every 3 decimal points. Now Tried to remove the 628 from the string. Now, realized that we need to convert the obtained groups of strings of length 3 to there ASCII values which gave out the required flag for the problem.
